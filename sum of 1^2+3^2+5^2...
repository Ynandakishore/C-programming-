#include<stdio.h>
int main()
{
	int sum=0,i,t,n;
	printf("enter the value of n:");
	scanf("%d",&n);
	for(i=1;i<=n;i++)
	{
		if(i%2!=0)
		{
			t=i*i;
			sum=sum+t;
		}
	}
	printf("sum of square odd number:%d",sum);
}
